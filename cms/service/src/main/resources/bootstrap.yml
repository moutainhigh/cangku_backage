# ${project.scenic.name}
spring:
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: 10.39.58.6:8849
        shared-dataids: nacos-config-example.properties,base-common.properties,common.properties
        refreshable-dataids: nacos-config-example.properties
        encode: UTF-8
        enabled: true